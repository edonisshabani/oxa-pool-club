import { MEMBERSHIP_CARD_COPY } from "../constants";
import type { InvitationCopy } from "./types";

export const POOL_CLUB_COPY: InvitationCopy = {
  inviteLabel: "Cordially invites",
  ...MEMBERSHIP_CARD_COPY,
};
